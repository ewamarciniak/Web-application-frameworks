<nav class="top-bar" id="nav">
  <div>
    <ul class="title-area">
      <li class="name">
        <h1><%= link_to "Diet Planner", root_path %></h1>
      </li>
    </ul>
    <section class="top-bar-section">
      <ul class="right">
        <li> <%= link_to "Home", site_index_path, :class => "#{'selected' if @tab=='site'}" %></li>
        <li> <%= link_to "Diet Planner", stats_index_path, :class => "#{'selected' if @tab=='planner' || @tab == 'profile'|| @tab == 'images' }" %></li>
        <li> <%= link_to "Healthy Recipes", recipes_path, :class => "#{'selected' if @tab=='recipes'}" %></li>
        <% if current_user %>
            <li id="logged_in">logged in as <%= current_user.email %></li>
            <li><%= link_to 'Logout', destroy_user_session_path, :method => :delete %></li>

        <% else %>
            <li><%= link_to 'Register',  new_registration_path(:user), :class => "#{'selected' if @tab=='register'}" %></li>
            <li><%= link_to 'Login', new_user_session_path , :class => "#{'selected' if @tab=='login'}" %></li>
        <% end %>
      </ul>
    </section>
  </div>
</nav>